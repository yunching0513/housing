# 這個專案的規矩

> 每一條都可以當場查證。查證方式：`python3 run_gates.py`
>
> 如果一條規則沒辦法用「指著某個檔案問：這樣算不算違反」得到明確答案，
> 它就不該寫在這裡。

---

## 0. 最重要的一條

**不准修改下列檔案。** 它們是這個專案的規則本身，不是實作細節：

```
AGENTS.md
CLAUDE.md
schemas/
checks/
run_gates.py
```

如果你認為某一條規則需要改，**先停下來，用文字說明為什麼，讓人類決定**。
擅自修改會被 `checks/gate_seal.py` 當場抓到。

> 這條規則存在的原因，這個專案有實際案例：
> `scripts/prep_pop_series.py` 的總數對帳曾經擋下來（109 年差 2,000），
> 處理方式是**把容差放寬**。理由是真的（來源單位是千人，四捨五入本來就有誤差），
> 但那次改動的性質是「遇到紅燈，改了檢查而不是改資料」，而且沒有任何機制
> 強迫說明。下一次可能就只是默默放寬。第 ⑤ 道閘門就是為了這件事存在。

---

## 1. 資料只能往一個方向流

```
data_TW/ 、 data/sources/     原始檔。只有 scripts/prep_*.py 讀得到
        ↓
scripts/prep_*.py             解析、對帳、寫檔
        ↓
data/tw_*.json                中間檔。這是版面唯一的資料來源
        ↓
scripts/build.py ＋ src/      內嵌
        ↓
dist/*.html                   單檔、離線可開
```

- `scripts/build.py` **不得**碰 `data_TW/`：版面不准直接讀原始檔。
- `scripts/prep_*.py` **不得**碰 `dist/`：解析程式不准回頭改產出。
- `data/` 底下的每一份 `tw_*.json` 都必須**有人產生、也有人用**。
  沒人產生的是手工檔（會過期），沒人用的是垃圾（會誤導）。

> 查證：`python3 checks/gate_flow.py`

---

## 2. 版面上的每一個統計數字都要有出處

- **不准把統計數字硬編碼進 `src/*.template.html`。** 數字一律從 `data/tw_*.json` 讀。
- 理由不是潔癖：手寫的數字在來源更新的那一天會靜靜地變成錯的，而且沒有人會發現。
- 每一頁都要標自己的**資料期**。跨資料比較之前要先看期別。
- 欄位的權威定義在 `schemas/`。**不准新增欄位、不准改欄位名稱、不准改型別。**

> 查證：`python3 checks/gate_contract.py`

---

## 3. 解析程式一律先對帳才寫檔

- 每一支 `prep_*.py` 在寫出 JSON 之前，必須先與原表的**小計列或合計列**對帳。
- 對不上就 `assert` 或 `sys.exit` 中止，**不准四捨五入到看起來對為止**。
- 讀進來的每一筆都要有交代：不是「納入」就是某一個具名的排除原因，
  兩者加總必須等於讀入筆數。
- 算不出來的數字給 `null` 並在頁面上標明，**絕對不要編一個數字**。
- 樣本數不足的分組給 `null`，不要給一個看起來像數字的雜訊。

> 查證：`python3 checks/gate_reconcile.py`

---

## 4. 不准增加執行門檻

- **不准 `pip install` 任何東西。** 只用 Python 內建函式庫。
  （現況：25 支腳本，需要安裝的套件 0 個。讀 xlsx 是自己拆壓縮檔讀的。）
- `dist/*.html` 必須是**單一檔案、零外部請求**：沒有 CDN、沒有外連字型、
  沒有 `fetch`、沒有外部圖片。斷網、離線、寄給別人都要能開。
- 不准要求 API key、不准要求資料庫、不准要求 Docker。

> 理由：一個需要安裝才能跑的檢查等於沒有檢查，沒有人會跑它。
> 一份需要連網才能開的圖表，寄給誰誰就打不開。
> 查證：`python3 checks/gate_offline.py`

---

## 5. 改動要小

- 優先改既有檔案，不要另開新檔案。
- 一次改動只做一件事。
- 不要順手重排 import、不要順手改格式、不要順手改無關的檔案。
- 要動「受保護檔案」以外的多個檔案時，**先列出清單讓人類確認**。

---

## 6. 寫給人看

- 註解與文件用**繁體中文**（臺灣用語）。變數與函式名用英文。
- 註解寫**為什麼**，不要重複程式碼在做什麼。
  特別是：為什麼選這個門檻、為什麼排除這一類、這個數字踩過什麼坑。
- 已知的限制要寫進程式碼與頁面，不要只留在對話裡。

---

## 這份檔案怎麼用

貼給 AI 的時候，連同這一句：

```
先讀 AGENTS.md，照它的規則做。做完跑 python3 run_gates.py，
把計分板貼回來給我看。如果有紅燈，修資料或修程式，不要修檢查。
```

> 這五道閘門的想法來自臺大計中 2026「從 Vibe Coding 到 Production Architecture」
> 的上機素材包（github.com/jeffhong824/vibe-coding），條文與實作依這個專案改寫。
